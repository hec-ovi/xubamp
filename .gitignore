/target
# Local third-party skins used only for testing; never shipped (clean-room policy).
/skins/
Cargo.lock.bak
*.swp
*~
.DS_Store
# Docker dev-build caches (cargo registry + target); never committed.
/.docker/
.research/
