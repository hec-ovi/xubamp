//! xubamp binary entry point.
//!
//! Phase 0 scaffold. The native-Wayland window, the PipeWire audio engine, and the
//! skin renderer are built up phase by phase; see `docs/ARCHITECTURE.md` for the plan.

fn main() {
    println!("xubamp {} (early scaffold)", env!("CARGO_PKG_VERSION"));
}
